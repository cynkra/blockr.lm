% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef-block.R
\name{new_coef_block}
\alias{new_coef_block}
\title{Coefficients Table Block}
\usage{
new_coef_block(conf_int = TRUE, conf_level = 0.95, ...)
}
\arguments{
\item{conf_int}{Logical. Include confidence intervals (default TRUE)}

\item{conf_level}{Numeric. Confidence level for intervals (default 0.95)}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A transform block object of class \code{coef_block} that outputs a data frame
with columns: term, estimate, std.error, statistic, p.value, and optionally
conf.low, conf.high.
}
\description{
Extract model coefficients from a linear model object using \code{\link[broom:reexports]{broom::tidy()}}.
Shows term names, estimates, standard errors, t-statistics, and p-values.
Optionally includes confidence intervals.
}
\examples{
if (interactive()) {
  library(blockr.core)
  model <- lm(mpg ~ cyl + hp, data = mtcars)
  serve(new_coef_block(), list(data = model))
}

}
