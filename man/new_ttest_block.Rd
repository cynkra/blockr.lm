% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ttest-block.R
\name{new_ttest_block}
\alias{new_ttest_block}
\title{T-Test Block}
\usage{
new_ttest_block(
  test_type = "independent",
  variant = "welch",
  dv = character(),
  group = character(),
  pair = character(),
  test_value = 0,
  ...
)
}
\arguments{
\item{test_type}{One of "one_sample", "paired", "independent".}

\item{variant}{For independent tests: "welch" or "student".}

\item{dv}{Numeric column name (the dependent variable / first sample).}

\item{group}{Categorical column with two levels (independent only).}

\item{pair}{Numeric column name (paired only).}

\item{test_value}{Numeric. Null mean for one-sample test.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A \code{ttest_block} transform block.
}
\description{
Adaptive block for one-sample / paired / independent t-tests. Emits an
\code{htest} object that downstream blocks (parameters, cohens_d, report)
consume via S3 dispatch.
}
